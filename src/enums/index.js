const genresEnum = require('./genres-enum')
const languagesEnum = require('./languages-enum')
const countryCodesEnum = require('./country-codes-enum')

module.exports = {
  genresEnum,
  languagesEnum,
  countryCodesEnum
}
